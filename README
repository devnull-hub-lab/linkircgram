A server-side relayer between IRC Server x Telegram Groups
linkircgram is a service that spawn each user of telegram group on the IRC channel

(In Development - WIP) Not Functional yet!!

Install from source

1. First, make sure the following dependencies are met:
 - json-c

cc -g -O0 -I/usr/local/include/ link.c parsing.c -o link -L/usr/local/lib -ljson-c
